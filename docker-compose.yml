version: '3.8'

services:
  app:
    build: .
    container_name: resource-depot
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - NODE_ENV=production
